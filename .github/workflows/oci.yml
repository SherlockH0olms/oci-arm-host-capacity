name: Oracle ARM Hunter (Debug Mode)

on:
  workflow_dispatch:
  schedule:
    - cron: '*/30 * * * *' 

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'

      - name: Install Dependencies
        run: composer install --no-dev

      - name: 🔍 BILGI KONTROLU (DEBUG)
        run: |
          echo "--- DEBUG BASLADI ---"
          if [ -z "${{ secrets.OCI_USER_OCID }}" ]; then echo "❌ HATA: OCI_USER_OCID bos!"; else echo "✅ OCI_USER_OCID dolu"; fi
          if [ -z "${{ secrets.OCI_TENANCY_OCID }}" ]; then echo "❌ HATA: OCI_TENANCY_OCID bos!"; else echo "✅ OCI_TENANCY_OCID dolu"; fi
          if [[ "${{ secrets.OCI_FINGERPRINT }}" != *":"* ]]; then echo "❌ HATA: Fingerprint formatı yanlış! (iki nokta : içermeli)"; else echo "✅ Fingerprint formatı doğru"; fi
          if [[ "${{ secrets.OCI_PRIVATE_KEY }}" != *"BEGIN"* ]]; then echo "❌ HATA: Private Key formatı yanlış! (BEGIN satırı eksik)"; else echo "✅ Private Key formatı doğru"; fi
          echo "Bölge: ${{ secrets.OCI_REGION }}"
          echo "--- DEBUG BITTI ---"

      - name: Create Private Key File
        run: |
          echo "${{ secrets.OCI_PRIVATE_KEY }}" > private_key.pem
          chmod 600 private_key.pem

      - name: Run Script
        run: php index.php
        env:
          OCI_REGION: ${{ secrets.OCI_REGION }}
          OCI_USER_ID: ${{ secrets.OCI_USER_OCID }}
          OCI_TENANCY_ID: ${{ secrets.OCI_TENANCY_OCID }}
          OCI_KEY_FINGERPRINT: ${{ secrets.OCI_FINGERPRINT }}
          OCI_PRIVATE_KEY_FILENAME: ./private_key.pem
          OCI_SUBNET_ID: ${{ secrets.OCI_SUBNET_ID }}
          OCI_IMAGE_ID: ${{ secrets.OCI_IMAGE_ID }}
          OCI_SSH_PUBLIC_KEY: ${{ secrets.OCI_SSH_PUBLIC_KEY }}
          OCI_AVAILABILITY_DOMAIN: ${{ secrets.OCI_AVAILABILITY_DOMAIN }}
          OCI_COMPARTMENT_ID: ${{ secrets.OCI_COMPARTMENT_ID }}
          OCI_SHAPE: VM.Standard.A1.Flex
          OCI_OCPUS: 4
          OCI_MEMORY_IN_GBS: 24
          OCI_MAX_INSTANCES: 1
